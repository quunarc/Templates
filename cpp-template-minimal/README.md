## How to use
- Use `git clone git@github.com:quunarc/Templates.git` to clone this repository.
- Remove tracking by `rm -rf .git/` to initialise your own repository.
- Allow direnv by `direnv allow` to handle loading the nix shell automatically on entering the directory.
- Adjust any project dependencies in `flake.nix`.
